### Note

- **Le bilan validé le 16 août et jamais parti a été envoyé le 2026-09-08 à
  11 h 09**, vérifié en base (ligne `Envoye` dans l'audit comme dans le journal
  de correspondance ; le portail sert donc « Mon bilan », qui exige cette
  ligne). Vingt-trois jours.
- **C'est la première observation réelle de `D-148`.** La séquence journalisée
  au même instant montre `Confirmation_Requise/Registre` rendu **`Non_envoye`**
  — « en attente de votre confirmation » —, puis `Envoye`, puis un
  `Confirmation_Requise/Renvoi` de nouveau **`Non_envoye`**. Trois lignes,
  **aucune fausse erreur**, là où les trois du 16 août affichaient « Échec
  d'envoi ».
- Les trois lignes du 16 août restent inchangées : le correctif vaut pour les
  envois à venir, il ne réécrit pas le journal.
