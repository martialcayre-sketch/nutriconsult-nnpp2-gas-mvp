### Modifié

- **La tête de reprise porte la nouvelle chaîne du `T0`** — cinq temps, les deux
  conditions dures qui la tiennent, les trois pièges fermés par construction, et
  le chiffre : neuf dossiers réels non confirmables tant que le second rideau
  n'est pas assigné, rendu, puis lu par une nouvelle synthèse.

### Documenté

- **Le défaut d'outillage trouvé en chemin** y est nommé : `visual-baselines`
  servait ses captures depuis `next dev` quand `ci.yml` les compare contre
  `next start`. Toute référence visuelle produite depuis la création de ce
  workflow portait l'indicateur Next.js — celles qui passaient le faisaient par
  la tolérance, pas par l'identité. La mesure de bruit du 2026-09-05
  (« WebKit 0 px ») décrivait donc un autre écart que celui-là.
